server:
  port: 9811
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/peng49/spring-cloud-config-repo
          basedir: peng49/spring-cloud-config-repo
          username: peng49
          password:
eureka:
  instance:
    instance-id: config-server-instance
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8300/eureka/


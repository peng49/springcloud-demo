spring:
  cloud:
    config:
      discovery:
        service-id: config-server
        enabled: true
      profile: dev
  application:
    name: product
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8300/eureka